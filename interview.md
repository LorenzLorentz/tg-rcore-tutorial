机考部分：
T2L4+T2L5的多核可展示性设计
目标
- 把你完成的T2L4+T2L5的合并作为你的Task4的基线，实现T4L45实验，具备T2L4和T2L5的功能柜
- T4L45支持内核响应中断
- T4L45支持多核处理和多核调度
- 设计测试用例来测试上述功能的正确性
- 设计相对复杂的测试能比较好地展现有了上面的功能后带来的性能变化
提示
- 可以从tg-rcore-tutorial-lab1开始逐步实现内核响应中断和多核支持，也可直接在你的T4L45上进行内核态响应中断和多核支持的设计与实现（相信自己与大模型合作的能力提升）。
- https://github.com/arceos-org/arceos 支持多核，是一个可以学习和参考的对象。
- 要求能理解你和大模型合作后的成果，特别是内核态响应中断的机制和多核处理的机制。
面试部分：
基于机考完成情况所设计OS相关的内核原理、设计与实现问题，包含架构设计，组件关联，核心技术等。涉及对操作系统的架构理解，知识点与实验的对应关系，知识点之间的关系，操作系统各个模块之间的关联关系，并行/并发/中断/信号（signal）/外设驱动对OS设计和运行的影响。
与老师助教面对面交流已完成的课程学习/实验内容。

1. 操作系统的架构理解
系统调用;
内存管理;
进程管理;
文件系统;
通信, 同步互斥;

(1) 操作系统启动
(2) 系统调用:
  `pub fn syscall(syscall_id: usize, args: [usize; 3]) -> isize`
  受控地从用户态到内核态, 进行高特权级操作 / 使用内核提供的服务.
    流程: 准备参数, 调用系统调用函数 -> 执行“陷入”执行 `ecall` -> 跳到内核预先设置好的 trap handler , 保存现场 (有什么要保存的? 用户寄存器, PC, 状态字) -> 进入内核, 写返回值, 恢复上下文
    (1) 普通函数
        (a) 特权级变化: 无变化;
        (b) 调用指令: jal;
        (c) 返回指令: ret;
        (d) 参数传递方式: 通用寄存器 (a0-a7) + 栈;
        (e) 上下文保存责任方: 编译器根据调用约定保存.
    (2) 系统调用:
        (a) 特权级变化: 从U到S;
        (b) 调用指令: ecall;
        (c) 返回指令: sret;
        (d) 参数传递方式: 通用寄存器 (a0-a5传参, a7传递系统调用号);
        (e) 上下文保存方式: 内核负责保存
    (3) SBI调用:
        (a) 特权级变化: 从S到M;
        (b) 调用指令: ecall;
        (c) 返回指令: mret;
        (d) 参数传递方式: 通用寄存器 (a0-a5传参, a7传递EID, a6传递FID);
        (e) 上下文保存方式: 底层固件负责保存.
(3) 内存管理
    要实现, 内存空间隔离与保护
    虚拟内存 <-> 物理内存

    虚拟内存机制:
        地址翻译 / 页表: 虚拟页号 -> 物理页号 + 偏移
        页表项中还会涉及相应内存页的权限问题: 是否有效? 是否可读、可写、可执行? 用户态是否能访问?
        缺页异常 -> 例子: Copy-On-Write

    进程的地址空间:
        从低地址到高地址: 代码段, 数据段, BSS段 (专门存放未初始化或初始化为零的全局变量和静态变量的静态内存区域), 堆, mmap区域 (?), 栈, 内核空间
        mmap用于: 共享库、文件映射、大块分配
        用户态分配: malloc, 通过移动堆指针, 以及mmap实现, 会涉及在用户态的自行的内存回收等管理
        内核态分配: ???
        mmap的功能: 
        (4) 进程管理
        “进程是资源分配的基本单位，是运行中的程序的一次实例”
        有独立的内核PCB (Process Control Block), 有自己独立的内存空间, 有自己独立的上下文, 还会涉及打开的文件
        因此会涉及trap handler相关等的内容
    
2. 完成的task2
  1. t2l4: 调度算法
  2. t2l5: 同步互斥
3. 多核模型
